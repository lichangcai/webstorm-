首先先打生成命令行的代码：1：生成公钥：ssh-keygen -t rsa -p
然后就不用设置用户名和密码这些的，直接跳过；
然后ssh-keygen -t rsa生成一些数据;
然后就是ls -a
这个再使用命令行cd .ssh/
再使用ls,
最后查看那个公钥的命令行是vi id_rsa.pub
查看私钥的vi id_rsa;
(如果是直接在页面直接查看密钥的话直接用cat id_rsa就可以了的)
或者是直接用 vi id_rsa.pub是直接查看公钥的，然后进入那个页面后（想要退出打的话，先按esc键，再按ship键，再输入那个q，按回车就可以使用了。如果没有修改的话就直接输入q!推出就可以的了，）
